{% extends '@SyliusAdmin/Crud/create.html.twig' %}

{% block content %}
    <div class="ui">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="ui primary button" type="submit">
            {{ 'sylius.ui.create'|trans }}
        </button>
        <a class="ui button" href="{{ path('dotit_sylius_ease_of_payment_plugin_admin_modality_index') }}">
            {{ 'sylius.ui.cancel'|trans }}
        </a>
        {{ form_end(form) }}
    </div>
{% endblock %}
